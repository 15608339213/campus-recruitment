# 秋招助手 v2.0 — 智能秋招一站式平台升级计划书

> **版本**：v2.0  
> **日期**：2026-08-07  
> **角色**：全栈开发工程师  
> **基于**：v1.0 已部署于腾讯云 http://120.53.31.101

---

## 一、对标项目调研

从 GitHub 调研了 7 个相关开源项目，作为 v2.0 升级的参考基准：

| 项目 | Stars | 核心价值 | 可借鉴方案 |
|------|-------|---------|-----------|
| **Reactive Resume** | 25k+ | 拖拽简历编辑器、模板库、AI 辅助写作、求职追踪面板 | 简历模板系统架构 + 多格式导出 |
| **OpenResume** | 6k+ | 从 PDF 导入解析简历、ATS 友好格式检测 | 简历解析算法、AI 分析简历可行性 |
| **ai-job-search** | 29k+ | AI 全流程求职 Agent（搜→评→投→跟）、技能差距热力图 | 求职仪表板设计、匹配评分逻辑 |
| **0voice/Campus_recruitment_interview_questions** | 4k+ | 1000+ 道企业真实面试题、50+ 企业覆盖 | 题库数据结构、按公司+方向分类 |
| **EasySpider** | 44k+ | 可视化无代码爬虫 | 降低招聘信息采集门槛 |
| **MediaCrawler** | 27.7k+ | 多平台异步爬虫 + Playwright 反反爬 | 爬虫架构参考、IP 代理池 |
| **SpringBoot+Vue 招聘系统** | 多个 | 基础业务模型 | 功能基线参考 |

**核心思路**：用 MediaCrawler 的思路做多源采集，用 Reactive Resume 做简历引擎，用 ai-job-search 做智能面板，用 0voice 扩充题库。

---

## 二、需求逐项分析与技术方案

### 2.1 首页岗位展示优化

**用户需求**：岗位数量太少，改为上下滚动分布，展示更多内容。

**现状问题**：
- 种子数据仅 300 条，用户感知"翻几页就没了"
- 当前分页 20 条/页，体验不够沉浸

**技术方案**：

| 维度 | 方案 |
|------|------|
| 数据量 | seed_data.py 从 300 条→**1000 条**，新增 50+ 企业 |
| 展示方式 | 改为**无限滚动**（Intersection Observer），每次加载 20 条，触底自动追加 |
| 布局优化 | 岗位卡片新增**企业 Logo**（CDN 图标）和**投递倒计时**，视觉更丰富 |
| 骨架屏 | 加载过程中展示骨架屏动画，提升感知速度 |
| 虚拟滚动 | 长列表用 `vue-virtual-scroller`，1000+ 条渲染无压力 |

**参考资料**：Reactive Resume 的拖拽卡片布局 + ai-job-search 的职位匹配度评分展示。

---

### 2.2 招聘数据源扩充 + 动态监控

**用户需求**：添加国家大学生就业平台等更多来源；招聘动态区域实时关注；自动拉取。

**新增数据源**（8 个维度）：

| 来源类型 | 具体平台 | 采集方式 |
|---------|---------|---------|
| 国家级平台 | 国家大学生就业服务平台 (ncss.cn) | API / 爬虫 |
| 高校就业网 | 清华/北大/浙大等 Top 20 高校就业信息网 | 爬虫 |
| 招聘网站 | Boss 直聘、智联招聘、前程无忧、国聘 | 已有 + 增强 |
| 企业官网 | BAT/TMD/华为/大疆等 100+ 企业校招页 | 爬虫 |
| 社区汇总 | GitHub awesome-campus-recruitment、牛客网、脉脉 | API / RSS |
| 公务员/编制 | 国家公务员局、各省人事考试网 | 爬虫 |
| 内推渠道 | 各企业内部内推码、内推帖 | 社区爬取 |
| 实习平台 | 实习僧、应届生求职网 | 爬虫 |

**招聘动态实时监控**：

- 新增 `crawl_logs` 表记录每次采集的时间、来源、新增数量、失败原因
- GitHub Actions 定时任务（每小时/每天），增量更新
- 首页新增「最新动态」板块：实时展示"X 分钟前新增 Y 条岗位"
- 用户可订阅关键词，新岗位匹配后邮件/站内通知

**技术难点与解决**：

| 难点 | 方案 |
|------|------|
| 反爬虫 | Playwright 模拟浏览器 + IP 代理轮换（参考 MediaCrawler） |
| 数据去重 | URL + 标题 + 公司 + 发布日期四维去重 |
| 数据清洗 | pipeline.py 增强：AI 自动提取薪资/地点/学历 |
| 结构化 | 非结构化 JD → AI 提取为结构化字段 |

---

### 2.3 个人资料板块全面升级

**用户需求**：导入寸照、上传简历、AI 分析简历、上传模板、AI 生成简历直接套用模板库。

**技术方案**：

#### a. 照片上传

- 前端：Naive UI Upload 组件，支持拖拽 + 裁剪（vue-advanced-cropper）
- 后端：FastAPI UploadFile，限制 5MB，自动压缩为 200×200
- 存储：本地 `uploads/avatars/` → 后续切腾讯云 COS

#### b. 简历上传 + AI 分析

```
用户上传 PDF/Word 简历
      ↓
PDF.js / python-docx 提取文本
      ↓
AI 分析（DeepSeek/Kimi/GLM）：
  • 识别教育背景、工作经历、技能栈
  • 评估 ATS 兼容性（参考 OpenResume）
  • 给出优化建议（缺了哪些技能、哪些描述不够量化）
      ↓
返回分析报告 + 自动填充个人资料
```

#### c. 简历模板库

| 模板来源 | 说明 |
|---------|------|
| 内置 10 套 | 参考 Reactive Resume 设计：经典/现代/创意/极简/学术 |
| 用户自定义上传 | 上传自定义 HTML/CSS 模板 |
| 社区共享 | 用户可公开发布模板，他人可一键使用 |

**模板数据结构**：
```
template: {
  id, name, category, preview_url, 
  html_structure, css_rules, 
  fields: ["name","email","education","experience",...],
  uploader_id, is_public, downloads
}
```

#### d. AI 简历生成 v2.0

**升级对比**：

| 维度 | v1.0 | v2.0 |
|------|------|------|
| 生成方式 | 纯 AI 生成 | AI 生成 → 自动填入模板 |
| 模板支持 | 无 | 10 套内置 + 自定义 + 社区 |
| 简历分析 | 无 | AI 诊断 + ATS 评分 |
| 多语言 | 仅中文 | 中英双语 |
| 导出格式 | PDF | PDF / DOCX / LaTeX / JSON |

**参考**：Reactive Resume 的拖拽编辑器 + OpenResume 的 ATS 检测。

---

### 2.4 AI 模型全面升级

**用户需求**：使用最顶尖模型，模型数量增加。

**新增 AI 模型**：

| 模型 | 厂商 | 优势 | API 兼容性 |
|------|------|------|-----------|
| GPT-4o / GPT-4.1 | OpenAI | 综合最强，代码/推理/创意 | OpenAI 格式 |
| Claude Sonnet 4 | Anthropic | 长文本理解，简历分析 | 需适配 |
| Gemini 2.5 Pro | Google | 免费额度大，多模态 | 需适配 |
| DeepSeek V3 / R1 | 深度求索 | 性价比最高，中文强 | OpenAI 格式（已有） |
| Qwen3-Max | 阿里 | 中文最强之一 | OpenAI 格式（已有） |
| GLM-4-Plus | 智谱 | 中文对话流畅 | OpenAI 格式（已有） |
| Kimi (Moonshot v2) | 月之暗面 | 超长上下文（128K） | OpenAI 格式（已有） |
| 豆包 (Doubao-pro) | 字节跳动 | 中文优化，速度快 | 需适配 |
| 文心一言 4.0 | 百度 | 中文理解深 | 需适配 |
| Llama 4 | Meta | 开源可私有化部署 | 需适配 |

**需要改造**：

- `backend/app/services/ai/provider.py` 的 `AIProviderClient` 需扩展为非 OpenAI 格式适配
- 新增各厂商的认证方式（API Key / Bearer / OAuth）
- 前端 AI 设置页面增加模型推荐标签（"推荐""免费""最强"等）

---

### 2.5 面试题库与笔试题库增强

**用户需求**：自定义题库源功能，基础题库上网查询扩充。

**技术方案**：

#### a. 基础题库扩充

当前 14 题 → 目标 **200+ 题**：

| 来源 | 数量 | 方式 |
|------|------|------|
| 0voice/Campus_recruitment_interview_questions | 抽取 50 题 | 手动导入 Markdown |
| LeetCode Hot 100 | 50 题（按企业标签整理） | API 拉取 |
| 牛客网面经 | 50 题 | 爬虫 |
| AI 生成 | 50 题 | 用 DeepSeek 按类别批量生成 |

**新增题库分类维度**：

```
旧：按岗位类别（技术/产品/运营/金融/设计）
新：岗位类别 × 公司（字节/腾讯/阿里/...）× 题型（笔试/面试/HR面）× 难度 × 年份
```

#### b. 自定义题库源

- 管理员可在后台**添加题库源 URL**
- 系统自动解析（markdown/json/csv 格式）
- 支持定时同步（如每周拉取最新面经）
- 用户可创建**个人错题本**和**刷题计划**

---

### 2.6 智能监控看板

**用户需求**：实时监控网站信息 + 反馈。

**独立监控看板**（参考 ai-job-search 的 html-report）：

| 面板 | 监控内容 | 刷新频率 |
|------|---------|---------|
| 服务健康 | CPU/内存/磁盘/容器状态 | 10 秒 |
| API 监控 | QPS、响应时间、错误率、慢查询 Top 10 | 1 分钟 |
| 用户数据 | 注册数、活跃用户、使用功能分布 | 实时 |
| 反馈看板 | 未处理/已处理反馈数、分类饼图 | 实时 |
| 爬虫状态 | 各数据源采集时间、成功率、新增数量 | 每次采集后 |
| 错误日志 | 5xx 错误聚合、异常堆栈告警 | 实时 |
| 安全告警 | 异常登录、暴力破解、SQL 注入拦截 | 实时 |

**技术选型**：

- **方案一（推荐）**：单文件 HTML + ECharts，定时轮询 `/api/v1/admin/stats`（已有 dashboard 基础）
- **方案二**：Grafana + Prometheus（功能强但部署重）

**管理员入口**：`/admin/dashboard`，仅 admin 角色可见。

---

### 2.7 安全性全面提升

**现状风险**：

| 风险 | 当前状态 | 严重程度 |
|------|---------|---------|
| HTTP 明文传输 | ❌ 无 HTTPS | 高 — 密码/Token 明文传输 |
| 文件上传无校验 | ⚠️ 基础实现 | 高 — 可能上传恶意文件 |
| 敏感信息暴露 | ⚠️ .env 含密钥已提交 | 中 — 泄漏风险 |
| 无 CSRF 保护 | ❌ | 中 |
| 无请求频率限制 | ⚠️ slowapi 基础 | 中 |
| SQL 注入 | ✅ SQLAlchemy ORM 防注入 | 低 |
| XSS | ✅ Vue 模板自动转义 | 低 |

**整改方案**：

| 措施 | 实现 | 优先级 |
|------|------|--------|
| **HTTPS** | Let's Encrypt 免费证书 + Nginx 配置 | P0 |
| **文件上传安全** | 校验 MIME 类型 + 文件大小限制 + 病毒扫描(ClamAV) | P0 |
| **敏感信息保护** | JWT Secret/REDIS_URL 用环境变量 + `.env` 不进 git | P0 |
| **CSRF Token** | 关键操作（生成简历/修改资料）加 CSRF Token | P1 |
| **速率限制增强** | 登录 5次/分钟，注册 3次/小时，API 100次/分钟 | P1 |
| **登录安全** | 失败 5 次锁定 30 分钟 + 验证码 | P1 |
| **审计日志** | 记录所有管理员操作 + 敏感操作 | P1 |
| **CORS 收紧** | 从 `["*"]` 改为白名单 | P1 |

---

### 2.8 数据模型扩展

v2.0 新增/变更的表：

```
# 简历模板
resume_templates: id, name, category, html_structure, css_rules, 
                   fields_json, preview_url, uploader_id, is_public, downloads

# 爬虫日志
crawl_logs: id, source, status, items_added, items_skipped, 
            error_msg, started_at, completed_at

# AI 分析结果
resume_analyses: id, user_id, resume_id, ats_score, skills_matched, 
                 missing_keywords, suggestions, raw_ai_response

# 题库源
question_sources: id, name, url, format, sync_interval, last_synced_at

# 用户订阅
user_subscriptions: id, user_id, keywords, locations, categories, 
                    notify_email, is_active

# 审计日志
audit_logs: id, user_id, action, resource, details_json, ip_address, created_at
```

---

## 三、v2.0 开发路线图

### 第一阶段：基础设施升级（第 1-2 天）

| 任务 | 产出 |
|------|------|
| HTTPS 证书配置 | 全站 HTTPS 加密 |
| 安全整改（CORS/限流/文件校验） | 安全基线达标 |
| 前端 Dockerfile 修复确认 | 登录/注册可用 |

### 第二阶段：岗位与数据（第 2-5 天）

| 任务 | 产出 |
|------|------|
| 首页无限滚动重构 | 沉浸式浏览体验 |
| seed_data 扩至 1000 条 | 岗位丰富度提升 |
| 国家大学生就业平台爬虫 | 新增国家级数据源 |
| 动态监控面板（管理员端） | 爬虫状态 + API 监控 |

### 第三阶段：简历引擎（第 5-10 天）

| 任务 | 产出 |
|------|------|
| 简历模板库（10 套 + 自定义） | 模板系统上线 |
| 简历上传 + AI 分析 | 诊断报告功能 |
| AI 模型扩展（GPT-4o/Claude/Gemini） | 10+ 模型可选 |
| PDF/DOCX/LaTeX 多格式导出 | 灵活导出 |

### 第四阶段：题库与社区（第 10-14 天）

| 任务 | 产出 |
|------|------|
| 题库扩充至 200+ 题 | 覆盖更多公司 |
| 自定义题库源 + 同步 | 灵活扩展 |
| 个人中心（照片/分析报告）| 用户档案完善 |

### 第五阶段：打磨上线（第 14-16 天）

| 任务 | 产出 |
|------|------|
| 智能监控看板 | 一站式运维面板 |
| 全功能回归测试 | 稳定运行 |
| 部署到腾讯云 | v2.0 上线 |

---

## 四、技术难点与应对

| 难点 | 风险 | 应对方案 |
|------|------|---------|
| 多源爬虫反爬 | 高 — 各平台反爬策略不断升级 | Playwright + IP 代理池 + 请求频率随机化 |
| AI 多模型适配 | 中 — 不同厂商认证和消息格式差异 | 统一抽象层 `BaseAIClient`，子类实现差异 |
| 简历解析准确率 | 中 — PDF 布局复杂多样 | pdfplumber 布局分析 + AI 辅助纠错 |
| 2核2G 服务器性能 | 中 — v2.0 服务增多 | Docker 资源限制 + CDN 静态资源 + Redis 缓存 |
| npm 构建 OOM | 低 — 前端打包可能内存不足 | swap 2G + `--max-old-space-size=1024` |

---

## 五、v1.0 与 v2.0 能力对比

| 维度 | v1.0 | v2.0 |
|------|------|------|
| 岗位数据 | 300 条种子 | 1000+ 条 + 8 源实时采集 |
| 展示方式 | 分页 20 条/页 | 无限滚动 + 骨架屏 |
| 简历 | AI 生成 + PDF 导出 | AI 生成 + 模板库 + 简历分析 + 多格式导出 |
| AI 模型 | 7 家 | 10+ 家（含 GPT-4o/Claude/Gemini） |
| 题库 | 14 题 | 200+ 题 + 自定义题库源 |
| 安全 | HTTP + 基础限流 | HTTPS + CSRF + IP 限制 + 审计日志 |
| 监控 | 无 | 智能运维看板 |
| 个人中心 | 基础信息 | 照片 + 简历分析 + 错题本 |
| 动态推送 | 无 | 实时招聘动态 + 关键词订阅 |
