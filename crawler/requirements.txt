# ============================================================
# 秋招爬虫子系统依赖
# 安装: pip install -r requirements.txt
# Playwright 浏览器内核需额外执行: python -m playwright install chromium
# ============================================================

# --- 异步 HTTP 客户端 ---
httpx==0.27.2

# --- HTML / Markdown 解析 ---
beautifulsoup4==4.12.3
lxml==5.3.0
markdown==3.7            # 解析 GitHub README Markdown

# --- 日期解析 ---
python-dateutil==2.9.0.post0

# --- 浏览器自动化（JS 渲染，可选）---
playwright==1.47.0

# --- 配置文件 ---
pyyaml==6.0.2

# --- 数据库写入（对齐后端 jobs 表）---
SQLAlchemy==2.0.35
aiosqlite==0.20.0        # SQLite 异步驱动（本地开发）
# asyncpg==0.29.0        # PostgreSQL 异步驱动（生产环境按需启用）
# psycopg2-binary==2.9.9 # PostgreSQL 同步驱动（爬虫 DB 写入用）
