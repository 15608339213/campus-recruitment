# 秋招助手 — 全栈网站项目计划书

> **项目定位**：一站式校园招聘信息聚合与 AI 辅助求职平台  
> **技术栈**：Vue 3 + FastAPI + PostgreSQL + Redis + Docker  
> **部署目标**：腾讯云轻量应用服务器，7×24 小时在线  
> **版本**：v1.1  
> **日期**：2026-08-06  
> **作者角色**：全栈开发工程师

---

## 一、项目概述

### 1.1 项目背景

校园招聘是每位大学生从校园走向职场的关键跳板，但现有求职平台存在三个核心痛点：

1. **信息碎片化**：岗位分散在智联招聘、Boss 直聘、国聘、企业官网、GitHub 社区仓库等数十个渠道，学生需要在不同平台反复切换，耗时巨大
2. **缺乏个性化工具**：市面上的简历生成工具多为模板填充，无法根据具体岗位要求生成针对性内容，缺乏 AI 驱动的定制化能力
3. **行业认知门槛高**：学生对不同行业、不同企业的招聘节奏、薪资水平、面试重点缺乏系统了解，信息不对称导致求职决策盲目

秋招助手的出发点很简单：让一个学生在找工作这件事上，打开一个网站就够了。

### 1.2 项目目标

| 目标 | 度量标准 |
|------|---------|
| 聚合 300+ 条校招/实习岗位 | 覆盖 90+ 企业、8 大行业、20 个城市 |
| AI 定制化简历生成 | 支持 7+ AI 服务商，基于岗位要求生成 STAR 法则简历 |
| 行业数据分析可视化 | 6 类图表（KPI 卡、趋势图、分布图、排行榜等） |
| 面试准备体系 | 5 个行业面试技巧 + 14+ 道各难度题库 |
| 全栈容器化部署 | 一条命令 `docker compose up -d` 完成部署 |
| 7×24 小时在线 | 基于腾讯云轻量服务器，不依赖第三方 PaaS 平台 |

### 1.3 目标用户

- **核心用户**：2026-2027 届应届毕业生，正在备战秋招
- **次要用户**：大二/大三学生，寻找实习机会
- **使用场景**：每天打开网站浏览新岗位 → 选定目标后生成定制简历 → 参考面试题库准备面试

---

## 二、技术架构设计

### 2.1 全栈技术选型

```
┌─────────────────────────────────────────────────────┐
│                    用户浏览器                          │
│              http://服务器IP (80端口)                  │
└─────────────────┬───────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────┐
│              Nginx (前端容器，80端口)                  │
│  • 静态资源服务 (/assets/* → 长期缓存)                │
│  • Vue Router History 模式支持                        │
│  • /api/* → 反向代理到后端 :8000                       │
└─────────────────┬───────────────────────────────────┘
                  │ Docker 内网 (campus-net)
┌─────────────────▼───────────────────────────────────┐
│         FastAPI 后端 (容器，8000端口)                  │
│  • JWT 双 Token 认证 (Access 30min + Refresh 7天)    │
│  • CORS 中间件                                        │
│  • 速率限制 (slowapi + Redis)                         │
│  • 统一 AI 客户端 (兼容 OpenAI 接口的任意服务商)        │
└──────┬────────────────────────────┬─────────────────┘
       │                            │
┌──────▼──────┐              ┌──────▼──────┐
│ PostgreSQL  │              │   Redis 7   │
│  (容器)     │              │  (容器)     │
│  :5432      │              │  :6379      │
│  数据持久化  │              │  缓存+限流   │
└─────────────┘              └─────────────┘
```

### 2.2 技术选型理由

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| 前端框架 | Vue 3.4 + Composition API | 渐进式框架，生态成熟，TypeScript 支持优秀 |
| UI 组件库 | Naive UI 2.39 | 组件丰富，Tree Shaking 友好，暗色主题原生支持 |
| 构建工具 | Vite 5.4 | 极快的冷启动和 HMR，生产构建基于 Rollup |
| CSS 方案 | Tailwind CSS 3.4 | 原子化 CSS，减少样式冲突，构建时体积优化 |
| 状态管理 | Pinia 2.2 | Vue 3 官方推荐，类型推断完善，模块化设计 |
| 图表 | ECharts 5.5 | 国内生态最好、文档最全的可视化库 |
| 后端框架 | FastAPI 0.115 | 异步原生支持，自动 OpenAPI 文档，Pydantic 数据校验 |
| ORM | SQLAlchemy 2.0 (async) | Python 生态最成熟的 ORM，2.0 异步 API 性能优秀 |
| 数据库 | PostgreSQL 15 | 功能最丰富的开源关系型数据库，JSON 字段支持灵活 |
| 缓存 | Redis 7 | API 限流计数器 + Token 黑名单 + 未来会话管理 |
| 容器化 | Docker + Docker Compose | 一条命令部署，环境一致性，依赖隔离 |
| 反向代理 | Nginx (Alpine) | 轻量高性能，前端静态服务 + API 代理二合一 |

### 2.3 目录结构

```
秋招网站/
├── frontend/                     # Vue 3 前端
│   ├── src/
│   │   ├── api/                  # 8 个 API 模块 (auth/job/resume/...)
│   │   ├── components/layout/    # 导航栏、侧边栏
│   │   ├── stores/              # Pinia 状态管理 (auth/job)
│   │   └── views/               # 11 个页面
│   ├── Dockerfile               # 多阶段构建 (Node → Nginx)
│   └── nginx.conf               # 反向代理 + History 模式
├── backend/                     # FastAPI 后端
│   ├── app/
│   │   ├── api/                  # 7 个路由模块 (30+ 端点)
│   │   ├── core/                # 配置/JWT/数据库/依赖注入
│   │   ├── models/              # 12 个 ORM 模型
│   │   ├── schemas/             # 7 个 Pydantic Schema 模块
│   │   └── services/            # 认证服务 + AI 服务 (7 家内置)
│   ├── Dockerfile               # 多阶段构建 (python:3.11-slim)
│   └── init_and_seed.py         # 启动时自动建表 + 300 条种子数据
├── crawler/                     # 爬虫模块 (6 个平台适配器)
├── scripts/                     # 初始化脚本 (init_db / seed_data)
├── campus-recruitment-dashboard/ # 独立 ECharts 仪表盘
├── docker-compose.yml           # 四服务编排 (postgres/redis/backend/frontend)
├── deploy.sh                    # 一键部署脚本
└── DEPLOY.md                    # 完整部署指南
```

---

## 三、功能模块详设

### 3.1 用户认证系统

**需求**：支持邮箱注册登录 + GitHub OAuth，保护用户数据安全。

**实现要点**：

| 特性 | 方案 |
|------|------|
| 密码存储 | bcrypt 哈希，passlib 库，成本因子使用默认值 |
| Token 机制 | JWT 双 Token：Access Token 30 分钟 + Refresh Token 7 天 |
| Token 传输 | httpOnly Cookie（防 XSS），SameSite=Lax |
| 邮箱验证 | 6 位数字验证码，5 分钟有效期，Redis 缓存 |
| 教育邮箱 | 额外支持 .edu 邮箱验证，标记学生身份 |
| OAuth | GitHub OAuth App 标准流程，authlib 库 |
| 登出 | Access Token 加入 Redis 黑名单，有效期 = token 过期时间 |

**数据流**：

```
注册: 邮箱+密码 → bcrypt 哈希 → 写入 users 表 → 发送验证邮件
登录: 邮箱+密码 → 验证哈希 → 生成双 Token → Set-Cookie
API 请求: Cookie → 提取 Token → 验证签名 → 注入 CurrentUser
Token 刷新: Refresh Token → 验证有效性 → 生成新双 Token
```

### 3.2 岗位聚合与检索

**需求**：聚合多源岗位数据，支持灵活筛选和搜索。

**实现要点**：

| 特性 | 方案 |
|------|------|
| 数据源 | 本地 seed_data.py 预置 300 条（覆盖 90+ 企业）+ 爬虫持续采集 |
| 筛选维度 | 关键词、企业类型、地点、岗位类别、类型（校招/实习）、学历、薪资区间、标签 |
| 排序 | 最新、薪资降序、截止日期升序、截止日期升序 |
| 分页 | offset/limit，默认 20 条/页，最大 100 条 |
| 收藏 | 用户可收藏岗位，查看收藏列表，添加备注 |
| 标签 | 每个岗位 2-4 个标签（六险一金、弹性工作、年终奖等） |

**SQL 查询优化**：

- 标签筛选使用子查询而非 JOIN，避免笛卡尔积
- is_active 字段建立索引，过滤无效岗位
- 搜索结果按 created_at DESC 排序，利用 B-tree 索引
- selectinload 预加载标签关联，避免 N+1 查询

### 3.3 AI 简历生成

**需求**：用户选择目标岗位，AI 生成针对性定制简历。

**实现要点**：

| 特性 | 方案 |
|------|------|
| 服务商管理 | 内置 7 家（DeepSeek/OpenAI/Kimi/GLM/Qwen/百川/零一万物）+ 支持自定义 |
| 统一接口 | `AIProviderClient` 类，兼容所有 OpenAI 接口格式的服务商 |
| API Key 安全 | 用户在前端配置后加密存入数据库，后端解密调用 |
| 生成策略 | 获取岗位信息 + 用户档案 → 构建 Prompt（含 STAR 法则指导）→ 调用 AI → 解析 JSON 返回 |
| 用量限制 | 每人每日 5 次（可配置），Redis 计数器 |
| 简历版本 | 支持版本管理，保留历史生成记录 |
| PDF 导出 | HTML 渲染 → WeasyPrint → PDF 下载 |

**Prompt 设计策略**：

```
系统角色：资深 HR + 简历优化师
输入信息：岗位 JD + 用户教育背景/技能/经历
输出格式：JSON (基本信息/求职意向/教育背景/技能/项目经历/自我评价)
生成要求：
  - 每段经历用 STAR 法则展开
  - 技能与岗位要求一一对应
  - 动词开头、结果量化
  - 控制一页纸
```

### 3.4 行业数据分析

**需求**：多维度可视化展示招聘市场全貌。

**图表设计**：

| 图表 | 类型 | 展示内容 |
|------|------|---------|
| KPI 指标卡 | 数字卡片 | 岗位总数、平均薪资、日均新增、活跃行业数 |
| 日新增趋势 | 折线图 | 近 60 天各行业每日新增岗位数 |
| 行业分布 | 饼图 | 八大行业岗位数量占比 |
| 企业类型分布 | 玫瑰图 | 民企/外企/国企/事业单位比例 |
| 地域分布 | 柱状图 | 各城市岗位数量 Top 15 |
| 行业薪资对比 | 分组柱状图 | 各行业平均薪资（带 min-max 区间） |
| 头部企业排行 | 排行榜 | Top 10 企业岗位数 + 平均薪资 |
| AI 职业推荐 | AI 生成文本 | 基于用户专业推荐行业方向和准备建议 |

### 3.5 面试题库与技巧

**需求**：按行业分类提供面试题和技巧指南。

**实现要点**：

- 题库分类：技术/产品/运营/金融/设计 5 大类
- 题型：笔试（算法/基础）、面试（专业）、HR 面（通用）
- 难度：easy / medium / hard
- 面试技巧：Markdown 格式长文，按行业分类
- 管理员可在后台增删改内容

### 3.6 用户反馈系统

**需求**：收集用户意见，持续改进产品。

- 4 种反馈类型：功能建议 / Bug 反馈 / 内容投诉 / 表扬
- 管理员可查看列表、查看详情、回复反馈
- 前端表单实时校验，提交后展示成功提示

---

## 四、数据库设计

### 4.1 核心实体关系

```
User (1) ─── (1) UserProfile       # 用户基本信息
User (1) ─── (N) OAuthAccount      # 第三方登录绑定
User (1) ─── (N) Resume            # AI 生成的简历
User (1) ─── (N) AIProviderConfig  # 用户配置的 AI Key
User (1) ─── (N) Favorite          # 收藏的岗位
User (1) ─── (N) Feedback          # 提交的反馈
Job  (1) ─── (N) JobTag            # 岗位标签
Job  (1) ─── (N) Favorite          # 被收藏的岗位
```

### 4.2 关键表字段说明

**users 表**：
```
id, email, password_hash, nickname, role(admin/user), 
is_active, is_verified, oauth_provider, created_at, updated_at
```

**user_profiles 表**：
```
user_id(FK), school, major, graduation_year, phone, bio, 
skills(JSON array), experience_json, projects_json, 
edu_email, edu_verified
```

**jobs 表**：
```
id, title, company, company_type, location, 
salary_min, salary_max, salary_unit, start_date, end_date,
job_category, job_type, degree_required, description_html,
source_url, source_repo, is_active, created_at, updated_at
```

**resumes 表**：
```
id, user_id(FK), target_job_id(FK), customized_content_json,
version, pdf_url, created_at
```

### 4.3 索引策略

| 表 | 索引 | 类型 | 用途 |
|----|------|------|------|
| jobs | is_active | B-tree | 过滤有效岗位 |
| jobs | job_category | B-tree | 行业筛选 |
| jobs | company_type | B-tree | 企业类型筛选 |
| jobs | created_at DESC | B-tree | 最新排序 |
| jobs | salary_max DESC | B-tree | 薪资排序 |
| favorites | (user_id, job_id) | UNIQUE | 防重复收藏 |
| resumes | (user_id, created_at) | B-tree | 用户简历列表 |

---

## 五、部署方案

### 5.1 当前状态

| 阶段 | 状态 | 说明 |
|------|------|------|
| 代码开发 | ✅ 完成 | 前端 11 页面 + 后端 30+ API 端点 |
| GitHub 仓库 | ✅ 完成 | https://github.com/15608339213/campus-recruitment |
| 本地测试 | ✅ 完成 | npm run dev + uvicorn 开发模式正常 |
| 种子数据 | ✅ 完成 | 300 条岗位 + 面试题库 + 技巧指南 |
| Docker 化 | ✅ 完成 | 多阶段构建，容器间网络互联 |
| 一键部署脚本 | ✅ 完成 | deploy.sh（自动安装 Docker + 构建 + 启动）|
| Vercel 部署（废弃）| ❌ | 国内网络无法访问 Vercel 节点 |
| 腾讯云部署 | 🔄 进行中 | 需要购买服务器并执行部署 |

### 5.2 腾讯云部署步骤

```
第一步：购买服务器
  → cloud.tencent.com/act/campus（学生优惠）
  → 2核2G 4M，Ubuntu 22.04，约 99 元/年

第二步：开放端口
  → 轻量服务器控制台 → 防火墙 → 添加 TCP 80

第三步：连接服务器
  → ssh ubuntu@服务器IP

第四步：安装 Docker
  → curl -fsSL https://get.docker.com | sudo sh
  → 配置腾讯云镜像加速

第五步：一键部署
  → git clone 项目
  → chmod +x deploy.sh && ./deploy.sh
  → 等待 5-10 分钟（首次构建镜像）

第六步：验证
  → 浏览器打开 http://服务器IP
  → 测试注册、登录、浏览、AI 生成等功能
```

### 5.3 费用概算

| 项目 | 费用 |
|------|------|
| 腾讯云轻量服务器 | 约 99 元/年（学生价，2核2G 4M） |
| 域名（可选） | 约 30-80 元/年 |
| HTTPS 证书 | 免费（Let's Encrypt） |
| AI API 调用 | 按量付费（DeepSeek 约 0.001 元/次） |
| 合计 | 约 100-200 元/年 |

---

## 六、项目进度与里程碑

### 6.1 已完成

| 里程碑 | 完成时间 | 产出 |
|--------|---------|------|
| 前后端开发 | 2026-07 | 完整功能实现，本地可运行 |
| 种子数据生成 | 2026-07 | 300+ 岗位、面试题库、技巧指南 |
| Docker 化 | 2026-07 | 多容器编排，一键构建 |
| GitHub 仓库 | 2026-08 | 代码托管、CI/CD 准备 |
| 部署文档 | 2026-08 | DEPLOY.md + deploy.sh + 项目计划书 |
| 仪表盘 | 2026-07 | 独立 ECharts 仪表盘（单文件 HTML） |

### 6.2 进行中

| 任务 | 预计完成 | 依赖 |
|------|---------|------|
| 购买腾讯云服务器 | 本周 | 学生认证 |
| 执行一键部署 | 购买后当天 | Docker 安装 |
| 功能验证测试 | 部署后 1 小时 | 部署完成 |
| 简历撰写（写入简历） | 部署后当天 | 功能验证通过 |

### 6.3 后续规划

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 购买域名并绑定 | 中 | 约 30 元/年，`qiuzhao.xxx` 更专业 |
| 配置 HTTPS | 中 | Let's Encrypt 免费证书，Nginx 集成 |
| 启用爬虫定时采集 | 高 | GitHub Actions 定时运行，增量更新数据库 |
| 移动端适配 | 中 | Tailwind responsive 已部分支持，需完善 |
| SEO 优化 | 低 | 添加 meta 标签 + sitemap，提升搜索引擎收录 |
| 提交搜索引擎 | 低 | 百度站长平台 + Google Search Console |

---

## 七、简历价值定位

部署完成后的项目可以在简历中呈现如下描述：

> **秋招助手 — 全栈网站开发项目**  
> *独立开发，2026.07 — 2026.08*
>
> - 基于 **Vue 3 + FastAPI** 开发一站式校园招聘平台，涵盖岗位聚合、AI 简历生成、行业数据分析和面试题库四大核心模块
> - 使用 **SQLAlchemy 2.0 异步 ORM** 设计 11 张数据表，实现用户认证、岗位检索、简历版本管理和反馈收集的完整业务闭环
> - 封装**统一 AI 客户端**，兼容 7 家 AI 服务商接口，支持用户自定义配置 API Key，实现岗位匹配度分析和 STAR 法则简历生成
> - 采用 **Docker Compose** 编排 4 个容器服务（Nginx + FastAPI + PostgreSQL + Redis），实现**腾讯云**一键部署和 7×24 小时稳定运行
> - 使用 **ECharts 5.5** 构建交互式数据分析仪表盘，呈现 60 天招聘趋势、行业分布、企业排行等多维度可视化图表
>
> 技术栈：Vue 3 · TypeScript · Naive UI · FastAPI · PostgreSQL · Redis · Docker · Nginx · ECharts

---

## 八、附录

### A. API 接口清单

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 认证 | POST | `/api/v1/auth/register` | 邮箱注册 |
| 认证 | POST | `/api/v1/auth/login` | 邮箱登录 |
| 认证 | GET | `/api/v1/auth/me` | 获取当前用户信息 |
| 认证 | GET | `/api/v1/auth/oauth/github` | GitHub OAuth 跳转 |
| 岗位 | GET | `/api/v1/jobs` | 岗位列表（支持 10+ 筛选参数） |
| 岗位 | GET | `/api/v1/jobs/{id}` | 岗位详情 |
| 岗位 | GET | `/api/v1/jobs/favorites/me` | 我的收藏 |
| 岗位 | GET | `/api/v1/jobs/meta` | 筛选选项元数据 |
| 简历 | POST | `/api/v1/resume/generate` | AI 生成简历 |
| 简历 | GET | `/api/v1/resume` | 简历列表 |
| 简历 | GET | `/api/v1/resume/{id}/pdf` | 下载简历 PDF |
| AI | GET | `/api/v1/ai-providers/builtin` | 内置 AI 服务商列表 |
| AI | POST | `/api/v1/ai-providers` | 添加自定义 AI 配置 |
| 分析 | GET | `/api/v1/analysis/stats` | 行业统计概览 |
| 分析 | GET | `/api/v1/analysis/recommend` | AI 职业推荐 |
| 面试 | GET | `/api/v1/interview/tips` | 面试技巧列表 |
| 面试 | GET | `/api/v1/interview/questions` | 题库列表 |
| 反馈 | POST | `/api/v1/feedback` | 提交反馈 |
| 系统 | GET | `/api/v1/health` | 健康检查 |

### B. 环境变量配置

| 变量 | 说明 | 生产环境值 |
|------|------|-----------|
| `APP_ENV` | 运行环境 | `production` |
| `DEBUG` | 调试模式 | `false` |
| `DATABASE_URL` | 数据库连接 | `postgresql+asyncpg://...` |
| `REDIS_URL` | Redis 连接 | `redis://redis:6379/0` |
| `SECRET_KEY` | JWT 密钥 | 32 位随机 hex |
| `DEEPSEEK_API_KEY` | AI 默认 Key | （可选） |
| `BACKEND_CORS_ORIGINS` | 跨域白名单 | `["*"]` |

### C. 常用运维命令

```bash
# 查看所有容器状态
docker compose ps

# 查看实时日志
docker compose logs -f

# 仅看后端日志
docker compose logs -f backend

# 重启所有服务
docker compose restart

# 停止所有服务（数据保留）
docker compose down

# 停止并清空数据（慎用）
docker compose down -v

# 更新代码并重新部署
git pull && docker compose up -d --build

# 备份数据库
docker exec campus-postgres pg_dump -U campus campus_recruitment > backup.sql

# 恢复数据库
docker exec -i campus-postgres psql -U campus campus_recruitment < backup.sql

# 查看容器资源占用
docker stats
```
